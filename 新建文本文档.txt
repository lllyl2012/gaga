动漫电影
create table animation(
id int auto_increment primary key comment '字段的注释',
name varchar(50) comment '字段的注释',
director varchar(30) comment '字段的注释',
create_time date comment '字段的注释',
language varchar(10) comment '字段的注释',
resolution_ratio varchar(5) comment '字段的注释',
format varchar(10) comment '字段的注释',
conntry varchar(20) comment '字段的注释',
subtitles varchar(5) comment '字段的注释',
bean_score varchar(5) comment '字段的注释',
size varchar(10) comment '字段的注释',
time varchar(10) comment '字段的注释',
remark varchar(255) comment '字段的注释'
)comment='表的注释';
电影——种类中间表
create table movie_category(
id bigint auto_increment primary key,
movie_id int comment '字段的注释',
category_id int comment '字段的注释'
)comment='表的注释'
种类
create table category(
id int auto_increment primary key,
name varchar(8)
)comment='表的注释'
图片
create table image(
id int auto_increment primary key,
movie_id int comment '字段的注释',
path varchar(255)
)comment='表的注释'
演员
create table performer(
id int auto_increment primary key,
movie_id int,
zh_name varchar(20)
)comment='表的注释'
语言
create table languages(
id int auto_increment primary key,
name varchar(20)
)engine=MyISAM;
alter table languages comment '语言';
国家
create table country(
id int auto_increment primary key,
name varchar(20)
)engine=MyISAM;
alter table country comment '国家';
分辨率
create table resolutionRatio(
id int auto_increment primary key,
name varchar(20)
)engine=MyISAM;
alter table resolutionRatio comment '分辨率';
格式
create table format(
id int auto_increment primary key,
name varchar(20)
)engine=MyISAM;
alter table format comment '格式';